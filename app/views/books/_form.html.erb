<%= form_with(model: @book, local: true) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :members %>
  <%= form.collection_check_boxes :member_ids, @followers, :id, :name %>

  <%= form.submit %>
<% end %>
